const frase = "Ser um bom programador requer muita prática";

const arrFrase = frase.split(" ");

for(let i = 0; i < arrFrase.length; i++) {
    console.log(arrFrase[i]);
}

const frase2 = "Mas isso não impede de você consultar no google ou stack overflow";

const arrFrase2 = frase2.split(" ");

for(let a = 0; a < arrFrase2.length; a++) {
    console.log(arrFrase2[a]);
}